import SwiftUI
import FettleUI

/// The whole app is `FettleUI`; this target exists only to be an entry point
/// and an app bundle.
@main
struct FettleApp: App {
    var body: some Scene { FettleScene() }
}
