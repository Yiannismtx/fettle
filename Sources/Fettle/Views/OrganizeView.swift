import SwiftUI
import FettleCore

struct OrganizeView: View {
    var body: some View {
        EmptyStateView(
            systemImage: "hammer",
            title: "OrganizeView",
            message: "Not built yet."
        )
    }
}
