import SwiftUI
import FettleCore

struct OverviewView: View {
    var body: some View {
        EmptyStateView(
            systemImage: "hammer",
            title: "OverviewView",
            message: "Not built yet."
        )
    }
}
